# 🤖 Торговый бот на Python

Полнофункциональный торговый бот для алгоритмической торговли на российском рынке с поддержкой трёх стратегий, бэктестингом и визуализацией результатов.

## ⚡ Основные возможности

### ✅ Три торговые стратегии
1. **Long Pullback** - Торговля на откатах в восходящем тренде
2. **Short Pullback** - Торговля на откатах в нисходящем тренде  
3. **Neutral Range** - Диапазонная торговля в боковике

### ✅ Автоматическое определение контекста рынка
- Использует ADX, EMA и направленные индикаторы (+DI/-DI)
- Автоматически переключается между стратегиями
- Закрывает позицию при изменении контекста

### ✅ Управление рисками
- Stop-loss и Take-profit на основе ATR (волатильности)
- **ОБЯЗАТЕЛЬНОЕ закрытие позиций на ночь!** ⚠️
- Кулдаун после срабатывания стопа (предотвращает повторный вход)
- Ограничение торгового времени

### ✅ Бэктестинг
- Полноценный бэктестинг на исторических данных
- Визуализация сделок на графике
- Детальная статистика: Win Rate, Profit Factor, Sharpe Ratio, Max Drawdown
- Экспорт логов сделок в CSV

### ✅ Модульная архитектура
- Чистый код с подробными русскими комментариями
- Легко добавлять новые стратегии
- Интерфейсы для расширения функционала

## 📦 Структура проекта

```
trading_bot/
├── config.py              # Конфигурация (параметры стратегий)
├── indicators.py          # Технические индикаторы (ATR, EMA, ADX)
├── market_context.py      # Определение контекста рынка
├── trading_engine.py      # Главный торговый движок
├── strategies.py          # Реализация трёх стратегий
├── backtester.py          # Бэктестинг с визуализацией
├── data_loader.py         # Загрузка данных (CSV, Tinkoff API)
├── main.py               # Точка входа в программу
├── requirements.txt      # Зависимости
└── README.md            # Документация
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Запуск бэктестинга

```bash
python main.py
```

Выберите опцию "1" для запуска бэктестинга на синтетических данных.

### 3. Просмотр результатов

После завершения бэктестинга будут созданы:
- `backtest_results.png` - График с точками входа/выхода
- `trade_log.csv` - Детальный лог всех сделок

## ⚙️ Настройка параметров

Все параметры находятся в файле `config.py`:

```python
# Временные ограничения
TRADING_START_TIME = time(10, 0)     # Начало торговли
TRADING_END_TIME = time(18, 30)      # Конец торговли
FORCE_CLOSE_TIME = time(23, 45)      # Закрытие позиций на ночь! ⚠️

# Управление рисками
STOP_LOSS_ATR_MULTIPLIER = 2.0       # Stop = ATR × 2
TAKE_PROFIT_ATR_MULTIPLIER = 3.5     # Take = ATR × 3.5

# Кулдаун после стопа
COOLDOWN_MINUTES = 15                # Минут до нового входа
```

## 📊 Использование с реальными данными

### Загрузка из CSV

```python
from data_loader import DataLoader

# Загрузка из файла
data = DataLoader.load_from_csv('your_candles.csv', date_column='timestamp')

# CSV должен содержать колонки: timestamp, open, high, low, close, volume
```

### Загрузка через Tinkoff API

```python
from data_loader import TinkoffDataLoader

loader = TinkoffDataLoader(token="YOUR_TOKEN")
data = loader.load_candles(
    figi="FUTXXXXX",      # FIGI инструмента
    days=10,
    interval='1min'
)
```

## 🎯 Создание собственной стратегии

```python
from trading_engine import TradingStrategy, Signal, OrderType
from market_context import MarketContext

class MyCustomStrategy(TradingStrategy):

    def on_candle(self, df, current_candle, context, atr):
        """Логика поиска точки входа"""

        # Ваша логика
        if условие_для_покупки:
            return Signal(
                action=OrderType.BUY,
                price=current_price,
                stop_loss=current_price - (atr * 2),
                take_profit=current_price + (atr * 3),
                reason="my_signal"
            )

        return None

    def should_close_position(self, df, current_price, position):
        """Проверка нужно ли закрыть позицию"""
        return False  # Или ваша логика выхода
```

## 📈 Интерпретация результатов

После бэктестинга вы получите следующие метрики:

- **Total Trades** - Общее количество сделок
- **Win Rate** - Процент прибыльных сделок (>50% хорошо)
- **Profit Factor** - Отношение прибыли к убыткам (>1.5 отлично)
- **Sharpe Ratio** - Доходность с учётом риска (>1.0 хорошо)
- **Max Drawdown** - Максимальная просадка (<20% приемлемо)

## ⚠️ ВАЖНО: Закрытие позиций на ночь

**Бот автоматически закрывает ВСЕ позиции перед ночью!**

Это критично для снижения рисков:
- Параметр `FORCE_CLOSE_TIME` в `config.py`
- По умолчанию: 23:45 (за 15 минут до окончания клиринга)
- Все открытые позиции будут закрыты по рынку

## 🔧 Параметры стратегий

### Лонг/Шорт на откатах
- Вход на уровнях Фибоначчи: 38.2%, 50%, 61.8%
- Stop-loss: цена входа ± (ATR × 2)
- Take-profit: цена входа ± (ATR × 3.5)

### Нейтральная стратегия
- Покупка около минимума диапазона (+ 0.4 ATR)
- Продажа около максимума диапазона (- 0.4 ATR)
- Диапазон рассчитывается за последние 5 дней

### Кулдаун после стопа
- Временной: 15 минут
- Ценовой: движение на 1.0 ATR в нужном направлении

## 📚 Дополнительные функции

### Сравнение стратегий

```bash
python main.py
# Выберите опцию "2"
```

Создаст сравнительную таблицу производительности всех стратегий.

### Просмотр лога сделок

После бэктестинга откройте `trade_log.csv`:

```csv
trade_num,entry_time,exit_time,side,entry_price,exit_price,pnl,exit_reason
1,2025-11-01 10:15:00,2025-11-01 11:30:00,long,100500.0,101200.0,665.0,take_profit
2,2025-11-01 14:20:00,2025-11-01 14:45:00,short,101000.0,100800.0,195.0,take_profit
...
```

## 🐛 Обработка ошибок

Бот включает обработку следующих ситуаций:
- Отсутствие данных с рынка → закрытие позиции
- Выход за торговое время → принудительное закрытие
- Изменение контекста → закрытие несоответствующих позиций
- Срабатывание стопа → активация кулдауна

## 📝 TODO / Планы развития

- [ ] Интеграция с Telegram для уведомлений
- [ ] WebSocket для получения данных в реальном времени
- [ ] Мультиинструментная торговля
- [ ] Машинное обучение для оптимизации параметров
- [ ] Trailing stop-loss
- [ ] Частичное закрытие позиций

## ⚖️ Лицензия и ответственность

**ВАЖНО:** Этот бот создан в образовательных целях!

- Торговля на финансовых рынках связана с рисками
- Автор не несёт ответственности за ваши убытки
- Тщательно тестируйте на исторических данных
- Начинайте с минимальных объёмов
- Используйте на свой страх и риск

## 🤝 Поддержка

При возникновении вопросов:
1. Проверьте комментарии в коде (они очень подробные)
2. Изучите примеры в `main.py`
3. Запустите бэктестинг на тестовых данных

## 🎓 Обучение

Рекомендуемый порядок изучения кода:

1. `config.py` - Понять параметры
2. `indicators.py` - Изучить индикаторы
3. `market_context.py` - Как определяется контекст
4. `strategies.py` - Логика стратегий
5. `trading_engine.py` - Как всё работает вместе
6. `backtester.py` - Тестирование
7. `main.py` - Запуск всего

---

**Удачной торговли! 📈🚀**
