# ============================================================
# ТОРГОВЫЙ БОТ - ЗАВИСИМОСТИ
# Протестировано на NumPy 2.2.6 + новейших scipy/scikit-learn
# ============================================================

# Основные библиотеки для работы с данными
pandas>=2.0.0
numpy>=2.0.0                  # ✅ NumPy 2.x (совместим с новыми scipy/sklearn)
matplotlib>=3.7.0

# Машинное обучение (для ML стратегий)
# ⚠️ Требуются ПОСЛЕДНИЕ версии для совместимости с NumPy 2.x!
scikit-learn>=1.5.0           # ✅ Поддержка NumPy 2.x с версии 1.5+
scipy>=1.13.0                 # ✅ Поддержка NumPy 2.x с версии 1.13+

# Работа с API Tinkoff Invest
tinkoff-investments>=0.2.0b9

# Дополнительные утилиты
python-dateutil>=2.8.0

# Для визуализации (необязательно, но рекомендуется)
seaborn>=0.12.0               # Красивые графики

# ============================================================
# ОПЦИОНАЛЬНЫЕ ЗАВИСИМОСТИ
# ============================================================

# Для асинхронной работы (если планируете расширение на real-time)
# aiohttp>=3.9.0
# websockets>=12.0

# Для оптимизации параметров
# optuna>=3.5.0                # Автоматический подбор параметров

# Для более продвинутого ML
# tensorflow>=2.15.0           # Нейронные сети (LSTM, etc)
# xgboost>=2.0.0               # Gradient Boosting

# ============================================================
# СОВМЕСТИМОСТЬ ВЕРСИЙ
# ============================================================
#
# NumPy 2.x совместим с:
#   - scikit-learn >= 1.5.0
#   - scipy >= 1.13.0
#   - pandas >= 2.0.0
#   - matplotlib >= 3.8.0
#
# Если у вас старые версии библиотек:
#   pip install --upgrade scipy scikit-learn
#
# ============================================================
# УСТАНОВКА
# ============================================================
#
# Полная установка (с ML):
#   pip install -r requirements.txt
#
# С обновлением всех пакетов:
#   pip install -r requirements.txt --upgrade
#
# Минимальная установка (без ML):
#   pip install pandas numpy matplotlib tinkoff-investments
#
# Проверка версий после установки:
#   pip list | grep -E "numpy|pandas|scikit|scipy|matplotlib"
#
# ============================================================
